import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Clock")
            HStack {
                Text("hour")
                Text("minute")
                Text("second")
            }
        }
    }
}
